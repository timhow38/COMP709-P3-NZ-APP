<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/navBar</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <!-- Global Font -->
        <item name= "android:textViewStyle" >@font/poppins_bold </item>
        <item name= "android:buttonStyle" >@font/poppins_bold </item>
    </style>
    <!-- Global Font -->

    <!-- Global BottomNav Style -->
    <style name="BottomNavigationTheme" parent="Theme.AppCompat.Light">
        <item name="android:background">@color/nav_background</item>
        <item name="colorPrimary">@color/nav_icon_active</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <!-- Global SeekBar Style
    <style name="AppThemeTwo" parent="Theme.AppCompat.Light">
        <item name="android:progressBackgroundTint">#f4511e</item>
        <item name="android:progressTint">#388e3c</item>
        <item name="android:colorControlActivated">#ffffff</item>
    </style>-->

    <!-- Global SplashScreen Style -->
</resources>
